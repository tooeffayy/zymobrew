.git
.gitignore
.dockerignore
Dockerfile
docker-compose.yml
*.md
.env*
/zymo
/dist
/bin
*.test

# Frontend artifacts: install + build happen inside the Bun stage.
# Anything from the host would just be wasted layer bytes (and risks
# stale builds leaking in via host node_modules / dist).
web/node_modules
web/dist
